/*
La operación misteriosa
Se tiene la expresión matemática siguiente: (20 + 10) / 5 + 3 * 2 - 4
*/

// --- PROCESO ---
// JavaScript respeta el orden de las operaciones (paréntesis, multiplicación/división, suma/resta).
// Por lo tanto, podemos escribir la expresión directamente en una sola línea.
let resultado = (20+10)/5+3*2-4*(20 + 10) / 5 + 3 * 2 - 4*(20+10)/5+3*2-4;


// --- SALIDA ---
// Mostramos el resultado final.
alert("El resultado de la operación es: " + resultado);